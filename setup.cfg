[importlinter]
root_packages =
    accounts
    orgs
    catalog
    learning
    orders
    config

[importlinter:contract:1]
name = orgs must not import accounts models directly
type = forbidden
source_modules =
    orgs
forbidden_modules =
    accounts

[importlinter:contract:2]
name = learning must not import catalog or orgs models directly
type = forbidden
source_modules =
    learning
forbidden_modules =
    catalog
    orgs

[importlinter:contract:3]
name = orders must not import accounts, catalog, learning, or orgs models directly
type = forbidden
source_modules =
    orders
forbidden_modules =
    accounts
    catalog
    learning
    orgs
ignore_imports =
    orders.services -> orgs.services

[importlinter:contract:4]
name = orgs must not import orders models directly
type = forbidden
source_modules =
    orgs
forbidden_modules =
    orders